# 未开启 Windows「开发人员模式」时无法创建 symlink；扁平安装 + 复制依赖
node-linker=hoisted
symlink=false
shamefully-hoist=true
# 部分镜像 tarball 不完整（如缺 mammoth/lib）；需要国内镜像时可改回 npmmirror 并确认包完整
registry=https://registry.npmjs.org/
