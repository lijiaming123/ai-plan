# 扁平安装到仓库根 node_modules，脚本用 ../../node_modules/... 解析（与各 app 的 cwd 一致）
# 注意：不要再设 symlink=false — 与 hoisted 组合时，各 app 下 .bin 常指向不存在的 apps/*/node_modules/*（tsx/vite/prisma 实际在根目录）。
# Windows 若 pnpm install 报 symlink 权限错误，请开启「设置 → 隐私和安全性 → 开发人员模式」。
node-linker=hoisted
shamefully-hoist=true
# 部分镜像 tarball 不完整（如缺 mammoth/lib）；需要国内镜像时可改回 npmmirror 并确认包完整
registry=https://registry.npmjs.org/
