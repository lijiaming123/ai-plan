# 生产构建或「开发直连 API」时使用（完整 origin，无尾斜杠）
# VITE_API_BASE_URL=https://api.example.com

# 开发：`pnpm dev` 默认使用相对路径 + Vite 代理到下方地址（与 apps/api 的 PORT 一致，默认 3000）
# VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# 设为 false 时，开发环境也使用 VITE_API_BASE_URL（需自行保证端口与后端一致）
# VITE_DEV_API_PROXY=false
